"""IMU WebSocket server ROS 2 package.

This package contains a ROS 2 node that subscribes to IMU data and
exposes it over a WebSocket server so that remote machines can
receive the IMU stream even when ROS 2 discovery does not work
across networks.
"""
