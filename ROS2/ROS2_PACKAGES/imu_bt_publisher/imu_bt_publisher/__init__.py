"""IMU WebSocket client ROS 2 package.

This package contains a ROS 2 node that connects to the IMU WebSocket
server (running on the Jetson Nano) and republishes the incoming
JSON IMU data as sensor_msgs/Imu messages on the PC.
"""
